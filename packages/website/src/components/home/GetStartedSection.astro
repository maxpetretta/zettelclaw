<section class="py-20" id="start">
  <p class="font-mono text-xs tracking-[0.2em] uppercase text-brand mb-4 reveal text-center">Get started</p>
  <h2 class="text-2xl md:text-3xl font-bold tracking-tight text-foreground mb-6 reveal text-center">
    Start in one or two commands
  </h2>
  <p class="text-muted-foreground leading-relaxed max-w-2xl mx-auto mb-8 reveal text-center">
    Run init for a fresh vault. If workspace `memory/` already exists, run migrate once to import it with per-file sub-agents plus a final `MEMORY.md`/`USER.md` synthesis pass.
  </p>

  <div class="max-w-lg mx-auto reveal">
    <div class="rounded-xl overflow-hidden border border-border bg-card shadow-2xl shadow-black/40">
      <div class="flex items-center gap-1.5 px-4 py-3 border-b border-border bg-muted">
        <span class="w-3 h-3 rounded-full bg-[#ff5f57]"></span>
        <span class="w-3 h-3 rounded-full bg-[#ffbd2e]"></span>
        <span class="w-3 h-3 rounded-full bg-[#28c840]"></span>
        <span class="flex-1"></span>
        <button
          onclick="navigator.clipboard.writeText('npx zettelclaw init\\nnpx zettelclaw migrate');const b=this;b.textContent='Copied!';setTimeout(()=>b.textContent='Copy',1500)"
          class="text-xs text-muted-foreground hover:text-foreground transition-colors px-2 py-0.5 rounded hover:bg-white/5 cursor-pointer"
        >
          Copy
        </button>
      </div>
      <div class="p-5 font-mono text-sm">
        <div class="flex items-center mb-2">
          <span class="text-brand select-none mr-2 shrink-0">$</span>
          <span class="text-foreground">npx zettelclaw init</span>
        </div>
        <div class="flex items-center text-muted-foreground">
          <span class="text-brand select-none mr-2 shrink-0">$</span>
          <span class="text-foreground">npx zettelclaw migrate</span>
          <span class="typing-cursor"></span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .typing-cursor {
    display: inline-block;
    width: 8px;
    height: 1.15em;
    background: var(--brand);
    margin-left: 3px;
    vertical-align: text-bottom;
    border-radius: 1px;
    animation: blink 1.1s step-end infinite;
  }

  @keyframes blink {
    0%,
    100% {
      opacity: 1;
    }

    50% {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .typing-cursor {
      animation: none !important;
      opacity: 1;
    }
  }
</style>
