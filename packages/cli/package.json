{
  "name": "zettelclaw",
  "version": "0.1.0",
  "description": "CLI and vault template for the Zettelclaw human+agent knowledge system",
  "type": "module",
  "bin": {
    "zettelclaw": "./bin/zettelclaw.js"
  },
  "files": [
    "bin",
    "dist",
    "vault"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --target node --format esm --outdir dist",
    "prepack": "bun run build",
    "start": "bun run src/index.ts",
    "init": "bun run src/index.ts init",
    "migrate": "bun run src/index.ts migrate",
    "lint": "biome check . && tsgo --noEmit",
    "lint:biome": "biome check .",
    "lint:typecheck": "tsgo --noEmit",
    "fix": "biome check --write ."
  },
  "dependencies": {
    "@clack/prompts": "1.0.1",
    "@zettelclaw/skill": "0.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.3",
    "@types/bun": "1.2.5",
    "@typescript/native-preview": "7.0.0-dev.20260219.1",
    "typescript": "5.9.3"
  }
}
